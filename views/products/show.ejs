<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>

    <ul>
        <li>Price: <%= product.price %> USD</li>

        <li>Category: 
            <!-- This is a link that allows us to see all the items related to the certain category -->
            <a href="/products?category=<%= product.category %>"><%= product.category %></a>
        </li>

        <li>Farm: <a href="/farms/<%= product.farm._id %>"><%= product.farm.name %></a></li>

    </ul>
    <a href="/products/<%= product._id %>/edit">Edit <%= product.name %></a>
    <br>
    <form action="/products/<%= product._id %>?_method=DELETE" method="POST">
        <button>Delete</button>
    </form>
    <a href="/products">All products</a>
</body>
</html>